import Option from "../../Messages/Option";

type Props = {};

const Messages = (props: Props) => {
  return (
    <div className="d-flex ms-auto col-12 col-lg-10 tw-h-screen">
      <Option />
    </div>
  );
};

export default Messages;
